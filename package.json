{
  "name": "@umbraco-commerce/mcp-server",
  "description": "An Umbraco Commerce MCP Server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "bin": {
    "peloton": "./build/index.js"
  },
  "scripts": {
    "build": "rimraf build && tsc",
    "start": "npm run build && dotenv -e .env -- node build/index.js",
    "inspect": "npm run build && dotenv -e .env -- mcp-inspector node build/index.js",
    "generate:api": "dotenv -e .env -- openapi-ts --file openapi-ts.config.js",
    "test:tools": "npm run build && dotenv -e .env -- mcp-server-tester tools tests/search-orders-test.yaml --server-config server-config.json",
    "test:compliance": "npm run build && dotenv -e .env -- mcp-server-tester compliance --server-config server-config.json"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "files": [
    "build"
  ],
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "^0.80.1",
    "@modelcontextprotocol/inspector": "^0.16.2",
    "@types/glob": "^8.1.0",
    "@types/node": "^24.1.0",
    "cross-env": "^10.0.0",
    "dotenv-cli": "^10.0.0",
    "glob": "^11.1.0",
    "mcp-server-tester": "^1.4.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "zod": "^3.25.76"
  }
}
